*intro.txt@zh*	Nvim


		  VIM 参考手册

介绍 Vim					*intro@zh* *nvim-intro@zh*

                                      输入 |gO| 查看目录。

==============================================================================
1. 简介							*intro-summary@zh*

Vim 是 "Vi IMproved"（Vi 增强版）的缩写。它是一个文本编辑器，包含了 Unix
程序 "Vi" 的大部分功能，并增加了许多新特性。它对于编辑程序和其他纯文本文件
非常有用。所有命令都通过键盘输入，这使得使用双手高效打字的人效率极高。

本文档介绍了 Vim 的基础知识。对于已经使用 Vi 的用户，请仔细浏览这些内容：
Vim 在一些微妙的地方与 Vi 有所不同。要了解 Vim 与 Vi 的区别，请参阅
|vi-differences|。

==============================================================================
2. Vim 的六种模式			*vim-modes-intro@zh* *vim-modes@zh*

Vim 有六种基本模式：

				*Normal@zh* *Normal-mode@zh* *command-mode@zh*
普通模式（Normal mode）		在普通模式下，你可以输入所有普通编辑命令。
				当你用一个命令启动编辑器时，就处于这个模式
				（如果你用 gvim 启动，可能处于 Easy 模式）。
				也称为命令模式。

可视模式（Visual mode）		类似于普通模式，但移动命令扩展高亮区域。
				当你使用非移动命令时，该命令作用于高亮区域。
				参见 |Visual-mode|。
				如果 'showmode' 选项打开，"-- VISUAL --" 显示
				在窗口底部。

选择模式（Select mode）		类似于 Microsoft Windows 中的选择。
				输入可打印字符会删除选中内容并进入插入模式。
				参见 |Select-mode|。
				如果 'showmode' 选项打开，"-- SELECT --" 显示
				在窗口底部。

插入模式（Insert mode）		在插入模式下，输入的文本会被插入到缓冲区中。
				参见 |Insert-mode|。
				如果 'showmode' 选项打开，"-- INSERT --" 显示
				在窗口底部。

命令行模式（Command-line mode）	在命令行模式下，你可以在窗口底部输入一行
				文本。这用于 Ex 命令（":"）、搜索模式
				（"/" 和 "?"）和过滤命令（"!"）。

终端模式（Terminal mode）	在终端模式下，所有输入（除了控制键的特殊
				组合）都被发送到在当前 |terminal| 缓冲区
				中运行的进程。

还有五种附加模式。这些是基本模式的变体：

			*Operator-pending@zh* *Operator-pending-mode@zh*
操作符待定模式			这类似于普通模式，但在输入了一个操作符命令
				后，Vim 在等待一个 {motion}（移动命令）来
				指定操作符将作用的文本。

替换模式（Replace mode）	替换模式是插入模式的特殊情况。你可以做和
				插入模式相同的事情，但每输入一个字符都会
				删除当前行上的一个字符。参见 |Replace-mode|。

虚拟替换模式			虚拟替换模式类似于替换模式，但不是替换文件
				中的字符，而是替换屏幕上的位置。
				参见 |Virtual-Replace-mode|。

插入普通模式			在插入模式下使用 CTRL-O 进入。类似于普通模式，
				但执行完一个命令后 Vim 返回插入模式。
				参见 |i_CTRL-O|。

插入可视模式			在插入模式下进入可视模式，例如使用 CTRL-O
				然后 "v"、"V" 或 CTRL-V。当可视选择结束后，
				返回插入模式。

插入选择模式			在插入模式下进入选择模式。例如使用鼠标拖拽
				或 CTRL-O 后跟一个选择模式命令。当选择模式
				结束后，返回插入模式。

==============================================================================
3. 切换模式						*mode-switching@zh*

从普通模式切换到其他模式：
	普通模式到插入模式:	|a| |A| |c| |C| |gI| |i| |I| |o| |O| |s| |S|
	普通模式到可视模式:	|v| |V| |CTRL-V|
	普通模式到选择模式:	|gh| |gH| |g_CTRL-H|
	普通模式到命令行模式:	|:| |/| |?| |!|
	普通模式到终端模式:	|:terminal|

返回普通模式：
	可视模式到普通模式:	<Esc>
	插入模式到普通模式:	<Esc> 或 CTRL-[ 或 CTRL-C
	命令行模式到普通模式:	<CR>（执行命令），<Esc> 或 CTRL-C（取消）
	终端模式到普通模式:	CTRL-\ CTRL-N

==============================================================================
4. 按键记法						*notation@zh*

<>		尖括号内的文字表示一个特殊按键名称。例如 <Up> 是向上箭头键。

CTRL-{char}	按住 CTRL 键再按 {char}。{char} 的大小写无关紧要；
		CTRL-A 和 CTRL-a 是等价的。

'option'	选项名称用单引号包裹。

"command"	命令引用使用双引号。

 vim:tw=78:isk=!-~,^*,^\|,^\":ts=8:noet:ft=help:norl:
