*insert.txt@zh*    Nvim


		  VIM 参考手册	  作者 Bram Moolenaar


						*Insert@zh* *Insert-mode@zh*
插入和替换文本					*mode-ins-repl@zh*

本文件主要介绍插入模式和替换模式。文末有一些以其他方式插入文本的命令。

                                      输入 |gO| 查看目录。

==============================================================================
1. 特殊按键						*ins-special-keys@zh*

在插入模式和替换模式中，以下字符有特殊含义；其他字符直接插入。
要在缓冲区中插入这些特殊字符，在它们前面加上 CTRL-V。

字符		作用 ~
-----------------------------------------------------------------------
						*i_CTRL-[@zh* *i_<Esc>@zh*
<Esc> 或 CTRL-[	结束插入或替换模式，回到普通模式。完成缩写。
		注意：如果你的 <Esc> 键不好按，试试 CTRL-[。

						*i_CTRL-C@zh*
CTRL-C		退出插入模式，回到普通模式。不检查缩写。
		不触发 |InsertLeave| 自动命令事件。

						*i_CTRL-@@zh*
CTRL-@		插入上次插入的文本并停止插入。

						*i_CTRL-A@zh*
CTRL-A		插入上次插入的文本。

						*i_<BS>@zh*
<BS> 或 CTRL-H	删除光标前的字符。

						*i_<Del>@zh*
<Del>		删除光标下的字符。如果光标在行尾，且 'backspace' 选项
		包括 "eol"（默认），则删除换行符；下一行将接在当前行后面。

						*i_CTRL-W@zh*
CTRL-W		删除光标前的单词（参见 |word-motions| 中关于"词"的定义）。
		默认情况下，删除前先设置一个新的撤销点。

						*i_CTRL-U@zh*
CTRL-U		删除当前行中在开始插入模式之后、光标之前输入的所有字符。
		如果没有新输入的字符且 'backspace' 不为空，
		则删除当前行中光标前的所有字符。
		默认情况下，删除前先设置一个新的撤销点。

						*i_<Tab>@zh*
<Tab> 或 CTRL-I	插入制表符。如果 'expandtab' 选项打开，
		则插入等量的空格。

						*i_<NL>@zh*
<NL> 或 CTRL-J	开始新行。

						*i_<CR>@zh*
<CR> 或 CTRL-M	开始新行。

						*i_CTRL-K@zh*
CTRL-K {char1} [char2]
		输入二合字母（参见 |digraphs|）。

CTRL-N		查找下一个关键词补全（参见 |i_CTRL-N|）。
CTRL-P		查找上一个关键词补全（参见 |i_CTRL-P|）。

						*i_CTRL-R@zh*
CTRL-R {register}
		插入寄存器的内容。在输入 CTRL-R 和第二个字符之间，
		会显示 '"' 来提示你需要输入寄存器名称。
		特殊寄存器：
			'"'	无名寄存器，包含最近删除或复制的文本
			'%'	当前文件名
			'#'	替代文件名
			'*'	剪贴板内容（X11：主选择）
			'+'	剪贴板内容
			'/'	最后的搜索模式
			':'	最后的命令行
			'.'	最后插入的文本
			'-'	最后的小段（少于一行）删除寄存器
			'='	表达式寄存器：提示你输入表达式
		参见 |registers| 了解更多关于寄存器的信息。

==============================================================================
2. 特殊特殊键					*ins-special-special@zh*

以下键可以在插入模式下退出插入模式，执行一个命令，然后返回插入模式。
这意味着你不必记住从插入模式切换到普通模式后再切回来。

						*i_CTRL-O@zh*
CTRL-O		执行一个普通模式命令，然后返回插入模式。

==============================================================================
3. 'textwidth' 和 'wrapmargin' 选项		*ins-textwidth@zh*

'textwidth' 选项可用于在插入文本时自动换行。当行长度超过 'textwidth'
的值时，Vim 会自动插入换行符。将 'textwidth' 设为 0 可关闭此功能。

==============================================================================
4. 'expandtab'、'smarttab' 和 'softtabstop' 选项
						*ins-expandtab@zh*

如果 'expandtab' 选项打开，当你按 <Tab> 键时插入空格而不是制表符。

如果 'smarttab' 选项打开：
- 当 <Tab> 在行首空白区域使用时，按 'shiftwidth' 的量插入空白。
- 在其他位置，使用 'tabstop' 或 'softtabstop' 的值。

==============================================================================
5. 替换模式						*Replace-mode@zh*

						*R@zh*
R		进入替换模式：输入的字符替换光标下的现有字符，每次一个，
		直到按 <Esc>。

						*gR@zh*
gR		进入虚拟替换模式：类似于替换模式，但替换的是屏幕位置
		而不是文件中的字符。

==============================================================================
6. 插入模式补全					*ins-completion@zh*

在插入模式下，可以使用多种方式补全关键词。

CTRL-N		在前向方向搜索下一个匹配的关键词。
CTRL-P		在后向方向搜索上一个匹配的关键词。

CTRL-X		进入 CTRL-X 子模式，可使用以下补全方式：

CTRL-X CTRL-L	整行补全。
CTRL-X CTRL-N	当前文件中的关键词。
CTRL-X CTRL-K	字典补全。
CTRL-X CTRL-T	同义词词典补全。
CTRL-X CTRL-I	当前文件和包含文件中的关键词。
CTRL-X CTRL-]	标签补全。
CTRL-X CTRL-F	文件名补全。
CTRL-X CTRL-D	定义或宏补全。
CTRL-X CTRL-V	Vim 命令行补全。
CTRL-X CTRL-U	用户定义的补全。
CTRL-X CTRL-O	全能补全（Omni completion）。
CTRL-X s	拼写建议。

==============================================================================
7. 插入模式命令					*inserting@zh*

以下命令从普通模式进入插入模式：

						*a@zh*
a		在光标后追加文本。如果光标在空行上，
		与 "i" 相同。

						*A@zh*
A		在行尾追加文本。

						*i@zh*
i		在光标前插入文本。

						*I@zh*
I		在行首第一个非空白字符前插入文本。

						*gI@zh*
gI		在第一列插入文本。

						*o@zh*
o		在当前行下方新开一行，并进入插入模式。

						*O@zh*
O		在当前行上方新开一行，并进入插入模式。

 vim:tw=78:isk=!-~,^*,^\|,^\":ts=8:noet:ft=help:norl:
