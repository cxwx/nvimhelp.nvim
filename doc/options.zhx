*options.txt@zh*    Nvim


		  VIM 参考手册	  作者 Bram Moolenaar


选项						*options@zh*

本文件介绍 Vim 中最常用的选项。

                                      输入 |gO| 查看目录。

==============================================================================
1. 设置选项					*set-option@zh*

						*:set@zh*
:set {option}={value}	设置字符串或数字选项的值。
:set {option}		打开布尔选项。
:set no{option}		关闭布尔选项。
:set {option}!		切换布尔选项。
:set {option}?		显示选项的值。
:set {option}&		重置选项为默认值。

==============================================================================
2. 常用选项					*common-options@zh*

						*'number'@zh*
'number'  'nu'		布尔	（默认关闭）
	显示行号。每行前面会显示行号。

						*'relativenumber'@zh*
'relativenumber'  'rnu'	布尔	（默认关闭）
	显示相对行号。每行前面显示相对于光标行的行号。

						*'wrap'@zh*
'wrap'			布尔	（默认开启）
	长行是否换行显示。关闭后，长行的超出部分不会显示。

						*'linebreak'@zh*
'linebreak'  'lbr'	布尔	（默认关闭）
	如果打开，Vim 会在适当的字符处换行（而不是在屏幕最后一列）。

						*'cursorline'@zh*
'cursorline'  'cul'	布尔	（默认关闭）
	高亮光标所在行。

						*'cursorcolumn'@zh*
'cursorcolumn'  'cuc'	布尔	（默认关闭）
	高亮光标所在列。

						*'tabstop'@zh*
'tabstop'  'ts'		数字	（默认 8）
	文件中一个 <Tab> 字符占据的空格数。

						*'shiftwidth'@zh*
'shiftwidth'  'sw'	数字	（默认 8）
	（自动）缩进每一步使用的空格数。用于 |>>|、|<<|、|cindent| 等。

						*'expandtab'@zh*
'expandtab'  'et'	布尔	（默认关闭）
	在插入模式下，用适当数量的空格替换 <Tab>。

						*'autoindent'@zh*
'autoindent'  'ai'	布尔	（默认开启）
	开始新行时，从当前行复制缩进。

						*'smartindent'@zh*
'smartindent'  'si'	布尔	（默认关闭）
	在插入新行时做智能自动缩进。适用于类 C 语言程序。

						*'ignorecase'@zh*
'ignorecase'  'ic'	布尔	（默认关闭）
	搜索模式中忽略大小写。也用于标签文件中的搜索。

						*'smartcase'@zh*
'smartcase'  'scs'	布尔	（默认关闭）
	覆盖 'ignorecase' 选项，当搜索模式包含大写字符时
	不忽略大小写。仅在 'ignorecase' 打开时使用。

						*'hlsearch'@zh*
'hlsearch'  'hls'	布尔	（默认开启）
	高亮所有匹配上次搜索模式的文本。

						*'incsearch'@zh*
'incsearch'  'is'	布尔	（默认开启）
	在输入搜索命令时，逐步显示匹配。

						*'scrolloff'@zh*
'scrolloff'  'so'	数字	（默认 0）
	光标上下方保留的最少屏幕行数。
	设为较大值（如 999）可使光标始终在屏幕中间。

						*'sidescrolloff'@zh*
'sidescrolloff'  'siso'	数字	（默认 0）
	光标左右方保留的最少屏幕列数。

						*'mouse'@zh*
'mouse'			字符串	（默认 "nvi"）
	启用鼠标支持。字符串中的字母决定在哪些模式下
	启用鼠标：
		n	普通模式
		v	可视模式
		i	插入模式
		c	命令行模式
		a	所有模式

						*'clipboard'@zh*
'clipboard'  'cb'	字符串	（默认 ""）
	设为 "unnamedplus" 可让 Vim 使用系统剪贴板，
	复制和粘贴与系统其他程序共享。

						*'undofile'@zh*
'undofile'  'udf'	布尔	（默认关闭）
	保存撤销历史到文件。即使关闭文件后再打开，
	仍然可以撤销之前的修改。

						*'backup'@zh*
'backup'  'bk'		布尔	（默认关闭）
	写入文件前创建备份文件。

						*'swapfile'@zh*
'swapfile'  'swf'	布尔	（默认开启）
	为缓冲区使用交换文件。允许在崩溃后恢复。

						*'hidden'@zh*
'hidden'  'hid'		布尔	（默认开启）
	允许隐藏已修改的缓冲区。切换缓冲区时不需要先保存。

						*'wildmenu'@zh*
'wildmenu'  'wmnu'	布尔	（默认开启）
	命令行补全增强模式。在命令行上方显示可能的匹配。

						*'completeopt'@zh*
'completeopt'  'cot'	字符串	（默认 "menu,preview"）
	控制插入模式补全的行为。常用值：
		menu	使用弹出菜单显示补全
		menuone	即使只有一个匹配也显示菜单
		noinsert	不插入任何文本直到选择
		noselect	不自动选择第一个匹配
		preview	在预览窗口显示额外信息

						*'signcolumn'@zh*
'signcolumn'  'scl'	字符串	（默认 "auto"）
	何时绘制标记列。常用值：
		"auto"	只在有标记时显示
		"yes"	始终显示
		"no"	从不显示

						*'termguicolors'@zh*
'termguicolors'  'tgc'	布尔	（默认关闭）
	在终端中使用 GUI 颜色（24 位颜色）。大多数现代
	终端都支持此选项。

 vim:tw=78:isk=!-~,^*,^\|,^\":ts=8:noet:ft=help:norl:
