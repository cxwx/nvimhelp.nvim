*motion.txt@zh*    Nvim


		  VIM 参考手册	  作者 Bram Moolenaar


光标移动						*cursor-motions@zh* *navigation@zh*

这些命令移动光标位置。如果新位置不在屏幕上，屏幕会滚动以显示光标
（另见 'scrolljump' 和 'scrolloff' 选项）。

一般说明：

如果你想知道在文件中的位置，使用 "CTRL-G" 命令 |CTRL-G| 或 "g CTRL-G"
命令 |g_CTRL-G|。如果设置了 'ruler' 选项，光标位置会持续显示在状态行中
（这会稍微减慢 Vim 的速度）。

有经验的用户喜欢用 hjkl 键，因为它们始终在手指下方。初学者通常更喜欢箭头键，
因为他们不知道 hjkl 键的作用。hjkl 的助记价值从键盘布局可以看出。
把 j 想象成一个向下指的箭头。

'virtualedit' 选项可以设置为允许将光标移动到没有字符或多列字符（如制表符）
内部的位置。

                                      输入 |gO| 查看目录。

==============================================================================
1. 移动和操作符					*operator@zh*

移动命令可以在操作符命令之后使用，让命令作用于移动经过的文本。即光标在移动
前后之间的文本。操作符通常用于删除或修改文本。可用的操作符如下：

	|c|	c	修改（change）
	|d|	d	删除（delete）
	|y|	y	复制到寄存器（yank，不改变文本）
	|~|	~	切换大小写（仅当 'tildeop' 被设置时）
	|g~|	g~	切换大小写
	|gu|	gu	转为小写
	|gU|	gU	转为大写
	|!|	!	通过外部程序过滤
	|=|	=	通过 'equalprg' 过滤或 C 缩进（如果为空）
	|gq|	gq	文本格式化
	|gw|	gw	文本格式化（不移动光标）
	|g?|	g?	ROT13 编码
	|>|	>	右移缩进
	|<|	<	左移缩进
	|zf|	zf	定义折叠
	|g@|	g@	调用 'operatorfunc' 选项设置的函数
						*motion-count-multiplied@zh*
如果移动命令包含计数，操作符之前也有计数，两个计数相乘。
例如："2d3w" 删除六个单词。
						*operator-doubled@zh*
双击操作符时作用于一行。使用计数（在第一个字符前或后），作用于那么多行。
因此 `3dd` 删除三行。第一个字符前后的计数相乘，`2y3y` 复制六行。

						*operator-resulting-pos@zh*
操作完成后，光标通常停留在被操作文本的开头。例如，"yfe" 不移动光标，
但 "yFe" 将光标向左移到复制开始的 "e" 处。

						*linewise@zh* *charwise@zh*
操作符要么影响整行，要么影响起始和结束位置之间的字符。通常，在行间移动
的移动命令影响行（按行操作），在行内移动的命令影响字符（按字符操作）。
但有一些例外。

						*exclusive@zh* *inclusive@zh*
按字符的移动要么是包含的（inclusive），要么是排除的（exclusive）。
包含时，移动的起始和结束位置都包含在操作中。排除时，靠近缓冲区末尾的
最后一个字符不包含在内。按行的移动始终包含起始和结束位置。

==============================================================================
2. 左右移动						*left-right-motions@zh*

这些命令将光标移动到当前行的指定列。它们在第一列和行尾停止，
"$" 例外，它可能移动到下一行。

							*h@zh*
h		或
<Left>		或
CTRL-H		或					*<BS>@zh*
<BS>			向左 [count] 个字符。|exclusive| 移动。

							*l@zh*
l		或
<Right>		或					*<Space>@zh*
<Space>			向右 [count] 个字符。|exclusive| 移动。

							*0@zh*
0			到行首第一个字符。|exclusive| 移动。

							*^@zh*
^			到行首第一个非空白字符。|exclusive| 移动。
			忽略计数。

							*$@zh*
$			到行尾。|inclusive| 移动。
			使用计数时，向下移动 count-1 行。
			在可视模式下，光标移到包含区域末尾行
			最后一个字符之后的位置。

							*g_@zh*
g_			到行尾最后一个非空白字符。|inclusive| 移动。

							*g0@zh*
g0		或
g<Home>			到屏幕行的第一个字符（当行被换行显示时，
			与 "0" 不同）。

							*gm@zh*
gm			到屏幕行中间的字符。

							*gM@zh*
gM			到文本行中间的字符。

							*g$@zh*
g$		或
g<End>			到屏幕行的最后一个字符。

							*bar@zh*
|			到第 [count] 列。

==============================================================================
3. 上下移动						*up-down-motions@zh*

							*k@zh*
k		或
<Up>		或
CTRL-P			向上 [count] 行。|linewise| 移动。

							*j@zh*
j		或
<Down>		或
CTRL-J		或
CTRL-N			向下 [count] 行。|linewise| 移动。

							*gk@zh*
gk		或
g<Up>			向上 [count] 个屏幕行（如果行被换行显示）。

							*gj@zh*
gj		或
g<Down>			向下 [count] 个屏幕行（如果行被换行显示）。

							*-@zh*
-  <minus>		向上 [count] 行，到第一个非空白字符。
			|linewise| 移动。

							*+@zh*
+		或
CTRL-M		或
<CR>			向下 [count] 行，到第一个非空白字符。
			|linewise| 移动。

							*_@zh*
_  <underscore>		向下 [count]-1 行，到第一个非空白字符。
			|linewise| 移动。

							*G@zh*
G			到第 [count] 行，默认最后一行。
			如果 'startofline' 未设置，保持原来的列位置
			或移到行尾（如果当前列超出行长度），
			否则移到第一个非空白字符。

							*gg@zh*
gg			到第 [count] 行，默认第一行。

							*:%@zh*
:%			等同于 1G 然后 G，即作用于整个文件。

==============================================================================
4. 单词移动						*word-motions@zh*

							*word@zh*
一个"word"（小写词）由一系列字母、数字和下划线组成，或者一系列其他
非空白字符组成，用空白（空格、制表符、换行符）分隔。参见 'iskeyword'。
空行也被视为一个词。

							*WORD@zh*
一个"WORD"（大写词）由一系列非空白字符组成，用空白分隔。
空行也被视为一个 WORD。

特殊情况：对于 "cw" 和 "cW" 命令，如果光标在非空白字符上，"w" 和 "W"
不包括该词之后的空白。

							*w@zh*
w			向前 [count] 个词。|exclusive| 移动。

							*W@zh*
W			向前 [count] 个大写词。|exclusive| 移动。

							*e@zh*
e			向前到第 [count] 个词的末尾。|inclusive| 移动。

							*E@zh*
E			向前到第 [count] 个大写词的末尾。|inclusive| 移动。

							*b@zh*
b			向后 [count] 个词。|exclusive| 移动。

							*B@zh*
B			向后 [count] 个大写词。|exclusive| 移动。

							*ge@zh*
ge			向后到第 [count] 个词的末尾。|inclusive| 移动。

							*gE@zh*
gE			向后到第 [count] 个大写词的末尾。|inclusive| 移动。

==============================================================================
5. 文本对象移动					*object-motions@zh*

							*(@zh*
(			向后 [count] 个句子。|exclusive| 移动。

							*)@zh*
)			向前 [count] 个句子。|exclusive| 移动。

							*{@zh*
{			向后 [count] 个段落。|exclusive| 移动。

							*}@zh*
}			向前 [count] 个段落。|exclusive| 移动。

							*]]@zh*
]]			向前 [count] 个节（section），或到下一个在
			第一列的 '{'。|exclusive| 移动。

							*][@zh*
][			向前 [count] 个节，或到下一个在
			第一列的 '}'。|exclusive| 移动。

							*[[@zh*
[[			向后 [count] 个节，或到上一个在
			第一列的 '{'。|exclusive| 移动。

							*[]@zh*
[]			向后 [count] 个节，或到上一个在
			第一列的 '}'。|exclusive| 移动。

==============================================================================
6. 文本对象选择					*text-objects@zh*

以下命令只能在操作符之后或可视模式下使用。"a" 命令选择"一个"对象
包括空白，"i" 命令选择"内部"对象不包括空白，或者只选择空白。

							*aw@zh*
aw			"一个词"，选择 [count] 个词（参见 |word|）。
			包括前导或尾随的空白，但不包括一行开头的空白。

							*iw@zh*
iw			"内部词"，选择 [count] 个词（参见 |word|）。
			词之间的空白也被算作一个词。

							*aW@zh*
aW			"一个大写词"，选择 [count] 个大写词（参见 |WORD|）。
			包括前导或尾随的空白。

							*iW@zh*
iW			"内部大写词"，选择 [count] 个大写词（参见 |WORD|）。

							*as@zh*
as			"一个句子"，选择 [count] 个句子（参见 |sentence|）。

							*is@zh*
is			"内部句子"，选择 [count] 个句子（参见 |sentence|）。

							*ap@zh*
ap			"一个段落"，选择 [count] 个段落（参见 |paragraph|）。

							*ip@zh*
ip			"内部段落"，选择 [count] 个段落（参见 |paragraph|）。

							*a]@zh* *a[@zh*
a] 或 a[		"一个 [] 块"，选择 [count] 个 '[' ']' 块。

							*i]@zh* *i[@zh*
i] 或 i[		"内部 [] 块"，选择 [count] 个 '[' ']' 块的内部。

							*a)@zh* *ab@zh*
a) 或 ab		"一个块"，选择 [count] 个 '(' ')' 块。

							*i)@zh* *ib@zh*
i) 或 ib		"内部块"，选择 [count] 个 '(' ')' 块的内部。

							*a>@zh*
a> 或 a<		"一个 <> 块"，选择 [count] 个 '<' '>' 块。

							*i>@zh*
i> 或 i<		"内部 <> 块"。

							*aB@zh* *a{@zh*
aB 或 a{		"一个大块"，选择 [count] 个 '{' '}' 块。

							*iB@zh* *i{@zh*
iB 或 i{		"内部大块"，选择 [count] 个 '{' '}' 块的内部。

							*at@zh*
at			"一个标签块"，选择 [count] 个标签块。

							*it@zh*
it			"内部标签块"。

							*a"@zh* *a'@zh* *a`@zh*
a" 或 a' 或 a`		"一个引用字符串"。

							*i"@zh* *i'@zh* *i`@zh*
i" 或 i' 或 i`		"内部引用字符串"。

==============================================================================
7. 标记						*mark-motions@zh*

跳转到标记可以用于移动命令（在操作符之后）。

							*m@zh*
m{a-zA-Z}		设置标记 {a-zA-Z} 为当前光标位置。

							*'@zh* *`@zh*
'{a-z}			到标记 {a-z} 所在行的第一个非空白字符。
`{a-z}			到标记 {a-z} 的精确位置。

'{A-Z}  `{A-Z}		到另一个文件中的标记（跨文件标记）。

一些特殊标记：
''  ``			到上一次跳转前的位置。
'.  `.			到最后一次修改的位置。
'^  `^			到最后一次插入模式停止的位置。
'[  `[			到最后一次修改或放置的文本的开头。
']  `]			到最后一次修改或放置的文本的末尾。
'<  `<			到可视区域的开头。
'>  `>			到可视区域的末尾。

==============================================================================
8. 跳转						*jump-motions@zh*

"跳转"是以下命令之一：|'|、|`|、|G|、|/|、|?|、|n|、|N|、|%|、|(|、|)|、
|[[|、|]]|、|{|、|}|、|:s|、|:tag|、|L|、|M|、|H| 以及开始编辑新文件的命令。

							*CTRL-O@zh*
CTRL-O			到跳转列表中第 [count] 个较旧的位置。

							*CTRL-I@zh*
CTRL-I		或
<Tab>			到跳转列表中第 [count] 个较新的位置。

							*:jumps@zh*
:jumps			打印跳转列表。

==============================================================================
9. 搜索						*search-commands@zh*

							*/@zh*
/{pattern}		向前搜索第 [count] 个匹配 {pattern} 的位置。

							*?@zh*
?{pattern}		向后搜索第 [count] 个匹配 {pattern} 的位置。

							*n@zh*
n			重复最近的搜索 [count] 次。

							*N@zh*
N			反方向重复最近的搜索 [count] 次。

							*star@zh*
*			向前搜索光标下的词 [count] 次。

							*#@zh*
#			向后搜索光标下的词 [count] 次。

							*gstar@zh*
g*			类似 "*"，但不要求完整单词匹配。

							*g#@zh*
g#			类似 "#"，但不要求完整单词匹配。

							*gd@zh*
gd			到光标下标识符的局部声明处。

							*gD@zh*
gD			到光标下标识符的全局声明处。

 vim:tw=78:isk=!-~,^*,^\|,^\":ts=8:noet:ft=help:norl:
